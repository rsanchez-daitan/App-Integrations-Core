spring:
  application:
    name: integration_bridge
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: ${CONSUL_PORT:8500}
      config:
        enabled: ${CONSUL_ENABLED:true}
        prefix: ${POD_ENV}/infra/${INFRA_NAME}
